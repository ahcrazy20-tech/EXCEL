# SheetX — خارطة طريق القوة والأداء (بحث محدّث أغسطس 2026)

> **1.3.0:** لوحات مؤشرات ورسوم وجداول بفلاتر مترابطة، وإصلاح ألوان خلفيات Excel والعناوين. [الدليل وحدود التنسيق والتحقق](docs/RELEASE_1.3.0.md).

> **تم تنفيذ المرحلة التالية في 1.2.0:** دمج الأوراق والبحث المرجعي والتنظيف القابل للتراجع، مع حفظ النتائج كوُرق مشتقة مستقلة. [الدليل والتحقق](docs/RELEASE_1.2.0.md). لوحات المعلومات مرحلة لاحقة.

> **الأولويات التالية:** [15 ميزة قوية مقترحة](docs/POWER_FEATURES.md). إصلاحات حذف الأوراق وشاشة Ask في [إصدار 1.1.1](docs/RELEASE_1.1.1.md).

> **خطة أحدث — سبتمبر 2026:** راجع [خطة مساحة التحليل الذكية](docs/INTELLIGENT_WORKSPACE_ROADMAP.md) للأولويات الجديدة وحدود التنفيذ ومعايير القياس. الأرقام الخارجية أدناه ليست قياسات لهذا التطبيق على الجهاز. تفاصيل إصلاح إعدادات AI في [سجل الإصلاح والتحقق](docs/AI_SETTINGS_FIX.md).

هذا الملف يجمع نتائج البحث حول المميزات الممكن إضافتها للتطبيق لتزيد **القوة** و**السرعة**،
مرتبة حسب الأثر مقابل حجم التنفيذ. العناصر في القسم الأول تم تنفيذها بالفعل في هذا التحديث.

---

## ✅ تم تنفيذه في هذا التحديث

| الميزة | الأثر |
|---|---|
| **كشف صف العناوين تلقائيًا** (`HeaderDetector`) | البرنامج يحدد صف العناوين بنفسه حتى لو كان تحت عنوان تقرير أو صفوف فارغة، ولا يعامله كبيانات. مع 3 أوضاع: كشف تلقائي / الصف الأول / بدون عناوين. |
| **ألوان الخلايا من Excel** | قراءة ألوان التعبئة (rgb / theme / indexed / tint) وعرضها داخل الجدول مع لون نص متكيّف (أبيض فوق الغامق). تُخزَّن بجدول جانبي مضغوط وتُحمَّل صفحة بصفحة — حتى ملايين الصفوف. |
| **جداول محورية (Pivot Tables)** | واجهة صفوف (1-2 مستويات) × أعمدة × قيمة (COUNT/SUM/AVG/MIN/MAX) فوق `GROUP BY` واحد — تعمل على ملايين الصفوف. مصفوفة مع خريطة حرارية وإجماليات، الضغط على أي خلية يفلتر الشيت بها، وتصدير CSV للنتيجة. (من قائمة ⋯ داخل أي ورقة) |
| **قفز فوري لأي صف (Keyset fast path)** | عند عدم وجود فلاتر، تحميل الصفحات صار بحثًا مباشراً بنطاق rowid بدل مسح الـ OFFSET — الذهاب للصف مليون فوري. |
| **تصدير HTML/PDF بالألوان** | خيار «جدول ملون بدل التقرير» في التصدير يبني جدول HTML حقيقي بألوان الخلايا المستوردة (متدفق بصفحات 2000 صف). |
| **حفظ تخطيط الورقة** | عرض الأعمدة والأعمدة المخفية تُحفظ لكل ورقة ولا تُفقد عند إعادة الفتح. |
| **كاش الـ Prepared Statements** في `Database` | كل عمليات التمرير/البحث/العد كانت تعيد تحضير الاستعلام في كل مرة. الآن تُحضَّر مرة وتُعاد استخدامها (حتى 64 استعلامًا) — وفق المعايير المنشورة هذا يرفع الإدراج إلى ~200 ألف صف/ثانية والقراءة أسرع بوضوح. |
| `PRAGMA optimize` بعد كل استيراد + `wal_autocheckpoint=1000` + `busy_timeout=5000` | إحصاءات مخطط الاستعلام تبقى طازجة، ومنع تضخم ملف WAL، وتقليل أخطاء القفل. |

---

## 🚀 أداء أعلى (اقتراحات مدروسة)

### 1. فهرس بحث FTS5 Trigram — بحث فوري بدل المسح الكامل
البحث الحالي `LIKE '%كلمة%'` يمسح كل صف. مع فهرس **FTS5 بتقطيع trigram** يصبح البحث عن
أي جزء من الكلمة فوريًا: قياس منشور على 18.2 مليون صفحو تحسّن من **1.76 ثانية إلى 14 مللي ثانية** (أكثر من 100×).
- يصلح جدولًا افتراضيًا لكل ورقة `data_N_fts` مع `tokenize='trigram', detail='none'` (نصف الحجم).
- البناء اختياري بعد الاستيراد (زر «فهرسة البحث») حتى لا يبطئ الاستيراد الأول.

### 2. استبدال LazyVStack بمحرك عرض قائم على UITableViewCell
قياس منشور (STRV): في القوائم الثقيلة سجل `List` (المبني على UITableView مع إعادة استخدام الخلايا)
تمريرًا خلال 5.5 ثانية مقابل **52.3 ثانية** لـ LazyVStack، مع ذاكرة أقل وـ 78 تجميدًا مقابل 4.6.
الجداول الحالية تستخدم LazyVStack داخل ScrollView — استبدال شريط الصفوف فقط بـ `UITableView`/`UICollectionView`
مع خلايا معاد استخدامها = أكبر قفزة سلاسة ممكنة مع مئات الأعمدة.

### 3. فهارس مركّبة تلقائية للفلاتر المتكررة
حاليًا يُنشأ فهرس لعمود الفرز فقط. حفظ «الفلاتر الأكثر استخدامًا» لكل ورقة وإنشاء
فهرس مركّب (`CREATE INDEX ... ON (c3, c7)`) لها يسرّع التصفية اليومية بشكل كبير.
(`PRAGMA optimize` + keyset pagination تم تنفيذهما بالفعل أعلاه.)

---

## 💪 مزايا قوة (المطلوبة في تطبيقات الجداول الحديثة)

حسب مقارنات 2026 لبرامج الجداول (Excel، Google Sheets، Zoho، WPS، LibreOffice) أهم ما يميز
المتقدّمين ويغيب عن SheetX حاليًا:

1. ~~**جداول محورية (Pivot Tables)**~~ — ✅ تم تنفيذها.
2. **تنسيق شرطي (Conditional Formatting)** — تلوين تلقائي: أعلى/أدنى 10%، أشرطة بيانات،
   تدرجات ألوان، تكرارات. يبنى فوق نظام الألوان المُنفَّذ الآن (`data_N_f`) دون تغيير التخزين.
3. **XLOOKUP / ربط ورقتين** — بحث قيمة من ورقة في أخرى (VLOOKUP أفقية). سهل التنفيذ فوق SQLite joins.
4. ~~**تصدير يحمل الألوان**~~ — ✅ تم تنفيذه (HTML/PDF).
5. ~~**حفظ عرض الأعمدة لكل ورقة**~~ — ✅ تم تنفيذه.
6. **رسوم بيانية أكثر** (خطي مزدوج، مبعثر، مساحي) على أي تجميعة — البنية موجودة في ChartsView.
7. **مساحة SQL محسّنة**: إكمال تلقائي لأسماء الأعمدة + استعلامات محفوظة مسماة (الجدول `meta_saved_queries` موجود وغير مستغل!).
8. **استيراد ملفات .xls القديمة** عبر تحويل داخلي (BIFF parsing) — أكثر طلب مفقود حاليًا.

---

## 📎 المصادر

- تحسينات إدراج SQLite (معاملات + WAL + Prepared + PRAGMA): macsqlclient.com/blog/50-how-to-improve-sqlite-insert-performance
- PRAGMA إنتاجية (wal_autocheckpoint ~12%): forwardemail.net/en/blog/docs/sqlite-performance-optimization-pragma-chacha20-production-guide
- قراءات SQLite عالية الأداء (mmap/cache/optimize): dev.to/lovestaco/high-performance-sqlite-reads-in-a-go-server-4on3
- إصلاح الاستعلامات البطيئة (ANALYZE/EXPLAIN/keyset): dbpro.app/blog/how-to-fix-slow-sqlite-queries
- FTS5 Trigram (100× أسرع لـ LIKE): andrewmara.com/blog/faster-sqlite-like-queries-using-fts5-trigram-indexes
- دليل FTS5 الكامل: mako.ai/guides/sqlite/full-text-search
- List مقابل LazyVStack (إعادة استخدام الخلايا): strv.com/blog/swiftui-list-vs-lazyvstack
- مقارنة برامج الجداول 2026 (Pivot/تنسيق شرطي/XLOOKUP): guideflow.com/blog/spreadsheet-software-tools • zapier.com/blog/best-spreadsheet-excel-alternative • lifewire.com/best-spreadsheet-apps-for-android-4178793

## Data preparation — 1.2.0

Cross-sheet joins, strict lookups, ordered cleaning with undo/redo, full-result diagnostics, and independent derived sheets with saved recipes. Imported data stays unchanged. [Usage, limits, safety, and validation](docs/RELEASE_1.2.0.md).
