# SheetX — عارض ومحلّل ملفات Excel الضخمة لـ iOS (TrollStore)

تطبيق iOS أصلي (SwiftUI + SQLite) يفتح ملفات **XLSX / CSV / TSV / JSON** الضخمة (ملايين الصفوف) بسلاسة،
ويسمح لك بأن تكتب أمرًا بالعربي أو الإنجليزي فيبحث ويجمّع ويعمل لك **تقريرًا** جاهزًا للمشاركة.

> مبني ليعمل على **iPhone 11 Pro Max / iOS 16.4 مع TrollStore** — يُبنى كـ IPA **غير موقّع** وتثبّته مباشرة.

---

## تحديث 1.1.0 — مساحة التحليل

- **⋯ → نظرة عامة على البيانات:** عدد مطابقات دقيق، اكتمال الخلايا، القيم المفقودة والمختلفة، ومقاييس رقمية مع توضيح العينة (حتى أول 10 آلاف صف مطابق).
- **اسأل → حفظ التحليل:** احفظ الخطة والفلاتر والحسابات، ثم أعد تشغيلها محليًا من **⋯ → التحليلات المحفوظة** دون مفتاح AI.
- تشغيل Ask والنظرة العامة على اتصال SQLite مستقل للقراءة فقط، مع إلغاء ومهلة وحدود نتائج؛ ولا يوقف الإلغاء الاستيراد أو قراءة الجدول.
- إصلاحات إعدادات AI مضمّنة. التفاصيل والحدود في [ملاحظات إصدار 1.1.0](docs/RELEASE_1.1.0.md).

## تحديث استقرار الذكاء الاصطناعي وخطة التطوير — سبتمبر 2026

- معالجة مسار محتمل لتجمّد الإعدادات: قراءة وحفظ مفاتيح Keychain خارج واجهة المستخدم، مع كاش وحالات تحميل وأخطاء واضحة.
- شاشة مستقلة لاختيار الموديل: بحث في كل القائمة التي يعيدها المزوّد، فلتر مجاني، واختيار يدوي بدون إنترنت — بدل عرض أول 40 موديلًا فقط.
- إلغاء طلبات الجلب والاختبار، مهلة طلب 20 ثانية، وعدم حفظ الإعدادات بمجرد اختبار الاتصال.
- دعم المزوّد المحلي بدون مفتاح، والحفاظ على المفتاح القديم عند فشل الحفظ.
- اختبارات وحدة وواجهة مضافة للمشروع وقالب CI؛ يحتاج تفعيلها آليًا إلى صلاحية تعديل workflow. **اختبار الجهاز الفعلي ما زال مطلوبًا**.

تفاصيل الإصلاح والتحقق: [AI Settings fix](docs/AI_SETTINGS_FIX.md).
خطة التطوير المقترحة (ليست ميزات منفذة): [Intelligent Workspace roadmap](docs/INTELLIGENT_WORKSPACE_ROADMAP.md).

## المزايا

**المحرك (السرعة)**
- استيراد **متدفّق (streaming)** لملفات XLSX: يفكّ ضغط ورقة العمل على القرص ويقرأ الـ XML عنصرًا عنصرًا (SAX) — لا يُحمَّل الملف كله في الذاكرة.
- كل ورقة تُفهرَس في **SQLite** (WAL + mmap + دفعات إدراج 20 ألف صف + **كاش Prepared Statements**) → التمرير والبحث والفرز فوري حتى مع ملايين الصفوف.
- قارئ CSV/TSV متوافق مع RFC‑4180 يقرأ على دفعات 4MB (يدعم الاقتباسات، الفواصل المختلفة، BOM، الترميز العربي UTF‑8).
- كشف تلقائي للفاصل، وأنواع الأعمدة (رقم / نص / تاريخ / منطقي)، وتحويل تواريخ Excel التسلسلية إلى تواريخ حقيقية.
- **كشف صف العناوين تلقائيًا**: البرنامج يحدد بنفسه أي صف هو صف العناوين — حتى لو كان تحت عنوان تقرير كبير أو صفوف فارغة — ويعامله كعناوين لا كبيانات (مع خيار «الصف الأول دائمًا» أو «بدون عناوين»).
- إنشاء فهارس (Indexes) عند الفرز/الفلترة لتسريع العمليات المتكررة.
- جدول بعرض افتراضي (Virtualized): تُحمَّل صفحات 200 صف عند الحاجة فقط، مع تحرير الصفحات البعيدة من الذاكرة.

**العرض**
- رأس أعمدة مثبّت، ترقيم صفوف، ألوان تبادلية، عرض/ارتفاع/خط قابل للتعديل.
- **ألوان خلايا Excel**: تُقرأ ألوان التعبئة من الملف (بما فيها ألوان theme و indexed مع tint) وتُعرض داخل الخلايا مع لون نص تلائي (أبيض فوق الألوان الغامقة) — بدون أي تأثير على الذاكرة حتى مع ملايين الصفوف.
- بحث فوري في كل الأعمدة، فلاتر متعددة (14 عاملًا: يساوي، يحتوي، أكبر من، بين، فارغ، ضمن قائمة…) مع AND/OR.
- ضغط على رأس العمود = فرز، إحصاءات العمود، أكثر القيم تكرارًا، وفلترة بضغطة.
- تفاصيل الصف الكاملة + نسخ.
- إخفاء/إظهار الأعمدة، والقفز إلى رقم صف (فوري حتى في الملايين بفضل مسار rowid المباشر).
- عرض الأعمدة والأعمدة المخفية **تُحفظ لكل ورقة**.
- تصدير HTML/PDF بخيار «جدول ملون» يحمل ألوان الخلايا الحقيقية.

**التحليل والتقارير**
- **جداول محورية (Pivot)**: من قائمة ⋯ داخل أي ورقة — اختر بُعد صف أو اثنين، وبُعد أعمدة اختياريًا، وقيمة (عدد/مجموع/متوسط/أدنى/أعلى) فيتبني المصفوفة كاملة بخريطة حرارية وإجماليات في `GROUP BY` واحد يعمل على ملايين الصفوف. الضغط على أي خلية يفلتر الورقة بها، والنتيجة تُشارك كـ CSV.
- **اسأل بياناتك**: اكتب أمرًا بالعربي أو الإنجليزي مثل:
  - `مجموع المبلغ حسب المدينة`
  - `أعلى 10 حسب الكمية`
  - `القيم المكررة في العميل`
  - `الصفوف اللي السعر أكبر من 1000`
  - `اعمل تقرير ملخص للشيت`
  - `Top 20 by Total` / `Count rows by Status`
- محرك محلي بالكامل (بدون إنترنت) يفهم الأمر ويحوّله إلى استعلام SQL آمن، **أو** استخدام مفتاح AI (OpenAI / Gemini / OpenRouter / أي API متوافق) لفهم أعقد.
- تقرير كامل تلقائي: عدد الصفوف، ملف تعريف كل عمود، مجموع/متوسط/وسيط/انحراف معياري/أدنى/أعلى، أكثر القيم شيوعًا، تجميعات، وفحص **جودة البيانات** (قيم فارغة، أعمدة ثابتة، قيم شاذة).
- رسوم بيانية (أعمدة / دائري / خطي) على أي تجميعة.
- تصدير: **CSV (بتدفق كامل بدون حد) / XLSX / JSON / Markdown / HTML / PDF** ومشاركة عبر شيت المشاركة.

**مزوّدو الذكاء الاصطناعي (كلهم لهم باقة مجانية — محدَّث أغسطس 2026)**

| المزوّد | مجاني؟ | الحد المجاني تقريبًا | مكان المفتاح |
|---|---|---|---|
| **Groq** (الافتراضي) | ✅ بدون بطاقة | ~30 طلب/دقيقة، حتى 14,400 طلب/يوم — الأسرع | console.groq.com/keys |
| **Google Gemini** | ✅ بدون بطاقة | موديلات Flash، سياق مليون رمز | aistudio.google.com/apikey |
| **Cerebras** | ✅ بدون بطاقة | ~1 مليون رمز/يوم | cloud.cerebras.ai |
| **Mistral** | ✅ الوضع المجاني افتراضي | حتى ~1 مليار رمز/شهر | console.mistral.ai/api-keys |
| **OpenRouter** | ✅ موديلات `:free` | ~20 طلب/دقيقة، 50 طلب/يوم | openrouter.ai/keys |
| **GitHub Models** | ✅ بتوكن GitHub | ~15 طلب/دقيقة، 50–150 طلب/يوم | github.com/settings/personal-access-tokens |
| OpenAI | 💳 مدفوع | — | platform.openai.com/api-keys |
| Custom | — | أي endpoint متوافق مع OpenAI (Ollama / LM Studio / بروكسي) | — |

- **مكان إدخال المفتاح:** الإعدادات → *مزوّد الذكاء الاصطناعي* → اختر المزوّد → الصق المفتاح → **اختبار** (يختبر القيم الحالية دون حفظها، ويعرض ✓ وزمن الاستجابة أو رسالة الخطأ بالتفصيل) → **حفظ** / **اجعله المزوّد الأساسي**.
- **زر «جلب الموديلات المتاحة»** يقرأ قائمة الموديلات الحقيقية من حسابك ويعلّم المجاني منها بشارة *مجاني*.
- **تبديل تلقائي (Fallback):** فعّل الخيار وستتم تجربة المزوّدين بالترتيب — لو Groq وصل حده أو وقع، يجرّب Gemini ثم Cerebras… بدون ما تعمل أي حاجة. ويظهر لك في النتيجة «تمت الإجابة عبر: …».
- كل مفتاح يُخزَّن منفصلًا في **Keychain**، وتقدر تحذفه بضغطة.
- التطبيق يشتغل **كامل بدون أي مفتاح** عبر المحرك المحلي؛ الـ AI إضافة اختيارية للأسئلة المعقّدة والتحليل السردي.

**الخصوصية**
- الاستيراد والحسابات تعمل على الجهاز. عند تفعيل AI لفهم السؤال تُرسل **أسماء الأعمدة وأنواعها وعدد الصفوف والسؤال** دون عينات صفوف. التحليل السردي يرسل مقتطفات من النتائج أو التقارير **قد تتضمن قيم خلايا**؛ وعند تفعيل التبديل التلقائي قد تُرسل هذه المعلومات إلى مزوّد آخر مفعّل. لا يُرفع الملف كاملًا.
- مفتاح الـ API يُحفظ في **Keychain**.

**الواجهة**
- عربي (RTL) + إنجليزي مع تبديل فوري، ووضع فاتح/داكن/تلقائي، واهتزاز اختياري.

---

## البناء (IPA غير موقّع لـ TrollStore)

### الطريقة الأسهل: GitHub Actions
ملف الـ workflow موجود في `ci/build-ipa.yml`. انسخه إلى مكانه الصحيح ثم ادفعه:

```bash
mkdir -p .github/workflows
cp ci/build-ipa.yml .github/workflows/build-ipa.yml
git add .github/workflows/build-ipa.yml
git commit -m "add IPA workflow"
git push
```

> ملاحظة: لم أستطع دفع الملف داخل `.github/workflows` مباشرة لأن صلاحية `workflows` غير ممنوحة لتطبيق GitHub المستخدم — لذلك وضعته في `ci/`.

بعد الدفع: افتح **Actions → Build unsigned IPA (TrollStore) → Run workflow**، وعند الانتهاء نزّل الملف من **Artifacts**:
`SheetX-unsigned-ipa → SheetX-unsigned.ipa`.

### البناء محليًا على macOS
```bash
brew install xcodegen
xcodegen generate
xcodebuild -project SheetX.xcodeproj -scheme SheetX -configuration Release \
  -sdk iphoneos -derivedDataPath build \
  CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" build

mkdir -p out/Payload
cp -R build/Build/Products/Release-iphoneos/SheetX.app out/Payload/
(cd out && zip -qry SheetX-unsigned.ipa Payload)
```

### التثبيت على الأيفون
1. انقل `SheetX-unsigned.ipa` إلى الجهاز (Files / AirDrop / تحميل مباشر).
2. افتح **TrollStore → + → اختر الملف → Install**.
3. التطبيق يظهر باسم **SheetX**، ويقبل فتح الملفات مباشرة من تطبيق "الملفات" (Open in / Share → SheetX).

---

## طريقة الاستخدام السريعة
1. اضغط **+** واستورد ملف (أو جرّب **تحميل بيانات تجريبية** = 20 ألف صف طلبات).
2. افتح الورقة → استخدم البحث والفلاتر، أو اضغط ✨ **اسأل** واكتب أمرك بالعربي.
3. من قائمة **⋯** → إنشاء تقرير كامل، ثم من تبويب **التقارير** شاركه PDF/HTML/Markdown.

---

## بنية المشروع
```
project.yml                 # مواصفات XcodeGen (iOS 16، ZIPFoundation، libsqlite3)
ci/build-ipa.yml            # GitHub Actions: بناء IPA غير موقّع
Sources/
  App/        SheetXApp, Library (الاستيراد), SheetViewModel (الصفحات), AppSettings + الترجمة
  Core/       Database (SQLite), Workspace (الفهرس), TableWriter, XLSXImporter, CSVImporter,
              QueryEngine (فلاتر/تجميعات/إحصاءات), NLQueryParser (أوامر عربي/إنجليزي),
              AIClient, ReportBuilder + Markdown, Exporter (CSV/XLSX/JSON/HTML/PDF), Keychain
  UI/         FilesView, SheetScreen, DataGridView, FilterViews, AnalysisViews (إحصاءات/رسوم/تصدير),
              AskView, ReportsView, SettingsView
  Support/    Info.plist, Assets.xcassets (الأيقونة)
```

## حدود معروفة
- ملفات `.xls` القديمة (Excel 97‑2003) غير مدعومة — احفظها كـ `.xlsx`.
- الصيغ (formulas) تُقرأ كقيمها المحسوبة المخزّنة داخل الملف.
- ألوان التعبئة تُعرض داخل التطبيق وتُصدَّر مع HTML/PDF (خيار الجدول الملون)، ولا تُصدَّر حاليًا مع XLSX.
- تعبئة اللون الأبيض الخالص تُهمل أثناء الاستيراد (لا تختلف بصريًا عن عدم التعبئة وتوفر تخزينًا كبيرًا).
- تصدير XLSX/JSON محدود بعدد صفوف (افتراضي 100 ألف) لحماية الذاكرة؛ CSV بلا حد.
